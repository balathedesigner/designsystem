import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function RatingPage() {
  return (
    <EmptyComponentTemplate
      title="Rating"
      description="Interactive component that allows users to view or select a rating from a set of icons, typically stars."
      componentName="Rating"
    />
  );
} 