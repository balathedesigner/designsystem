import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function AutocompletePage() {
  return (
    <EmptyComponentTemplate
      title="Autocomplete"
      description="Input component that provides suggestions as the user types, enabling faster and more accurate data entry."
      componentName="Autocomplete"
    />
  );
} 