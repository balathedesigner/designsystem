import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function SliderPage() {
  return (
    <EmptyComponentTemplate
      title="Slider"
      description="Interactive control that allows users to select a value or range from a continuous set of options."
      componentName="Slider"
    />
  );
} 