import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function DataGridPage() {
  return (
    <EmptyComponentTemplate
      title="Data Grid"
      description="Advanced table component for displaying and interacting with large datasets, featuring sorting, filtering, and pagination."
      componentName="DataGrid"
    />
  );
} 