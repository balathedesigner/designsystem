import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function VideoPage() {
  return (
    <EmptyComponentTemplate
      title="Video"
      description="Component for displaying video content with controls for playback, volume, and other media features."
      componentName="Video"
    />
  );
} 