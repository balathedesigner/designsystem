import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function PopoverPage() {
  return (
    <EmptyComponentTemplate
      title="Popover"
      description="Floating containers that display contextual information or UI elements triggered by user interaction."
      componentName="Popover"
    />
  );
} 