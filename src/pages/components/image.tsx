import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function ImagePage() {
  return (
    <EmptyComponentTemplate
      title="Image"
      description="Component for displaying images with various options for responsive behavior, placeholders, and fallbacks."
      componentName="Image"
    />
  );
} 