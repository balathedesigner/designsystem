import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function FileUploadPage() {
  return (
    <EmptyComponentTemplate
      title="File Upload"
      description="Component for selecting and uploading files with support for validation, drag and drop, and progress indication."
      componentName="FileUpload"
    />
  );
} 