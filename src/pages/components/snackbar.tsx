import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function SnackbarPage() {
  return (
    <EmptyComponentTemplate
      title="Snackbar"
      description="Brief message that appears at the bottom of the screen to provide feedback about an operation."
      componentName="Snackbar"
    />
  );
} 