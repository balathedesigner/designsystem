import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function ComboboxPage() {
  return (
    <EmptyComponentTemplate
      title="Combobox"
      description="Combined input and dropdown lists that allow users to filter and select from a collection of options."
      componentName="Combobox"
    />
  );
} 