import React from 'react';
import { EmptyComponentTemplate } from '@/components/shared/EmptyComponentTemplate';

export default function GalleryPage() {
  return (
    <EmptyComponentTemplate
      title="Gallery"
      description="Grid-based display of images with options for thumbnails, lightbox viewing, and navigation controls."
      componentName="Gallery"
    />
  );
} 